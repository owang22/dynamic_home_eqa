"""Grounding data for the attribute-first household set: census marginals
(load_acs) and time-use survey timing distributions (load_atus). The
generation prompt receives SAMPLED NUMBERS from these, so the LLM writes
around real survey timings instead of inventing them."""
