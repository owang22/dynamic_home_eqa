# revamp_v1 households — ChatGPT-5.6sol (high reasoning)

Created **August 7, 2026**.

Narrative household profiles for the revamped simulation set, generated by
**ChatGPT-5.6sol with high reasoning effort** and pasted in by hand. Same
10-household spec and verbatim prompt as the sibling `claude-fable-5/` set —
see `../generation_prompt.md` and `control.yaml` for the household list and
generation status.

Stage 1 only: WHO lives in each home and WHAT objects they own. No clock
times or hour-by-hour routines; the schedule layer comes in a later step.

Status: all 10 households GENERATED (`households/hh_001`–`hh_010`), pasted
in verbatim as produced — hh_001 in one message, hh_002–hh_010 in a second.

As pasted, this set wrote its inventories as flow mappings (`{id: ...,
class: ...}` — except hh_001, which came through in block style) and quoted
its prose fields. `../normalize_profiles.py` has since rewritten all ten
into the shared canonical style documented in `../README.md`; content is
unchanged apart from two object ids that did not carry their class prefix
(`medication_ruth` → `medication_bottle_ruth` in hh_003, `medication_grace`
→ `medication_bottle_grace` in hh_007). Re-run the script after pasting in
any further households.
